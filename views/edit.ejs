<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="relative bg-gradient-to-br from-zinc-900 via-zinc-800 to-black text-white min-h-screen">

    <a href="/profile" class="fixed top-5 ml-4 bg-green-600 hover:bg-green-700 transition px-4 py-2 text-sm rounded-md shadow">
       Go to Profile
    </a>



    <a href="/logout"
        class="fixed top-5 right-5 z-50 bg-red-500 hover:bg-red-600 transition duration-200 rounded-md px-4 py-2 text-sm font-medium shadow">
        Logout
    </a>

    <!-- Main Container -->
    <div class="max-w-2xl mx-auto px-4 py-10">
        <!-- Post Form -->
        <div class="bg-zinc-800 p-6 rounded-xl shadow-lg border border-zinc-700 mb-10">
            <h5 class="text-lg font-semibold mb-3">Edit Post</h5>
            <form action="/update/<%= post._id %>" method="post" class="space-y-4">
                <textarea placeholder="What's on your mind?" name="content" rows="3"
                    class="w-full resize-none bg-zinc-900 border border-zinc-600 rounded-lg px-4 py-3 focus:outline-none focus:border-blue-500 transition duration-200 shadow-sm placeholder-zinc-400"><%= post.content %></textarea>
                <input type="submit" value="Edit"
                    class="w-full px-4 py-2 bg-green-600 hover:bg-green-700  rounded-lg font-medium transition duration-200 cursor-pointer shadow" />
            </form>
        </div>
      
    </div>

</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Feed</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen">
  <div class="max-w-2xl mx-auto p-6">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold">📰 Feed</h1>
      <a href="/profile" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md">My Profile</a>
    </div>

    <% if (posts.length===0) { %>
      <p class="text-zinc-400 text-center">No posts yet.</p>
      <% } else { %>
        <div class="space-y-6">
          <% posts.forEach(post=> { %>
            <div class="bg-zinc-800 p-5 rounded-lg shadow border border-zinc-700">
              <div class="flex items-center justify-between mb-3">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 rounded-full overflow-hidden border">
                    <img src="/images/uploads/<%= post.user.profilepic %>" alt="<%= post.user.username %>'s Profile"
                      class="w-full h-full object-cover">
                  </div>
                  <h4 class="text-blue-400 font-semibold">@<%= post.user.username %>
                  </h4>
                </div>
                <span class="text-sm text-zinc-500">
                  <%= new Date(post.date).toLocaleString('en-US', { dateStyle: 'medium' , timeStyle: 'short' }) %>
                </span>
              </div>

              <p class="text-zinc-300">
                <%= post.content %>
              </p>
            </div>
            <% }) %>
        </div>
        <% } %>
  </div>
</body>

</html>